%p#notice= notice
%h1 Listing Courses
%table
  %thead
    %tr
      %th Code
      %th Title
      %th Details
      %th Credit hours
      %th Semester
      %th Instructor
      %th User
      %th{colspan: "3"}
  %tbody
    - @courses.each do |course|
      %tr
        %td= course.code
        %td= course.title
        %td= course.details
        %td= course.credit_hours
        %td= course.semester
        %td= course.instructor
        #TODO: list users (students) attending course
        %td= link_to 'Show', course
        %td= link_to 'Edit', edit_course_path(course)
        %td= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Course', new_course_path
